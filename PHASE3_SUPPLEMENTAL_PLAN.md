# แผนเสริมการพัฒนาระบบ Phatlada (Phase 3 & Advanced Security Architecture)

เอกสารฉบับนี้เป็น **แผนเสริมการพัฒนา** ตามคำสั่ง เพื่อใช้เป็นแนวทางมาตรฐานในการพัฒนา 5 ฟังก์ชันขั้นสูง พร้อมการเปิดโหมดความปลอดภัย Supabase RLS และการควบคุมแบบ Modal-Driven

---

## 1. ขอบเขต 5 ฟังก์ชันหลัก

1. **ระบบจดมิเตอร์น้ำ-ไฟ และออกใบแจ้งหนี้ (Utility Meter & Invoice Generator)**:
   - บันทึกเลขมิเตอร์น้ำและไฟฟ้าประจำเดือน (เปรียบเทียบเลขมิเตอร์เดิมและใหม่)
   - คำนวณยอดเงินรวม (ค่าเช่าห้อง + ค่าน้ำ + ค่าไฟ + ค่าส่วนกลาง) อัตโนมัติ
   - สร้าง QR Code พร้อมเพย์มาตรฐาน EMVCo ให้สแกนชำระเงินได้ทันที
2. **ระบบพิมพ์ใบแจ้งหนี้และใบเสร็จรับเงิน (Print & PDF Export)**:
   - แสดงตัวอย่างเอกสารขนาด A4 และ Slip 80mm
   - สั่งพิมพ์ (`window.print()`) จัดรูปแบบหน้ากระดาษทางการ ซ่อนแถบเมนูที่ไม่เกี่ยวข้องอัตโนมัติ
3. **ระบบความปลอดภัย Supabase จริง และการแบ่งสิทธิ์ (RLS & RBAC)**:
   - เปิด `ENABLE ROW LEVEL SECURITY;` ทุกตารางในฐานข้อมูล
   - กำหนดสิทธิ์แยกตาม 3 บทบาท:
     - **OWNER (เจ้าของ)**: ควบคุมได้ทุกอย่าง ตั้งค่าระบบ ลบข้อมูล จัดการสมาชิก
     - **MANAGER (ผู้จัดการ)**: จัดการห้อง ผู้เช่า สัญญาเช่า ออกบิล รับชำระเงิน
     - **STAFF (เจ้าหน้าที่/แม่บ้าน)**: จดมิเตอร์น้ำ-ไฟ ดูแลงานแจ้งซ่อม ไม่มีสิทธิ์ลบข้อมูลสำคัญหรือดูงบการเงิน
   - ซ่อน/ปิดการใช้งานปุ่มตามสิทธิ์ของผู้ใช้งาน
4. **ระบบหลายหอพักใน 1 บัญชี (Multi-Dormitory Switcher)**:
   - สลับหอพักที่ต้องการดูแลจากเมนูมุมซ้ายบน
   - รองรับการเพิ่มหอพักใหม่
   - แยกข้อมูลห้องพัก ผู้เช่า และบัญชีการเงินของแต่ละหอพักออกจากกันอย่างชัดเจน
5. **การแจ้งเตือนแบบ Real-time และการสร้างหน้า Notifications & Help**:
   - หน้า `/notifications` เต็มรูปแบบ: กรองหมวดหมู่งานซ่อม, การเงิน, สัญญาเช่า และปุ่มทำเครื่องหมายอ่านทั้งหมด
   - หน้า `/help` หน้ารวมคู่มือการใช้งานระบบ คำถามที่พบบ่อย (FAQ) สำหรับเจ้าของหอพัก
   - เชื่อมต่อ Supabase Realtime Channels แจ้งเตือนแบบสดโดยไม่ต้องกดรีเฟรชหน้าจอ

---

## 2. กฎเหล็กในการพัฒนา (Core Architectural Rules)

- **ปุ่มกดต้องเป็น Modal**: ทุกปุ่มคำสั่ง (Action Button) จะเปิดหน้าต่าง Modal เพื่อกรอกข้อมูล ยืนยัน หรือแสดงตัวอย่างเอกสาร
- **ขนาดไฟล์ไม่เกิน 800 - 1,000 บรรทัด**: แยกโมดูล คอมโพเนนต์ และเซอร์วิสออกเป็นไฟล์ย่อย
- **ปลอดข้อมูลตัวอย่าง (Zero Mock Data)**: ใช้โครงสร้างข้อมูลจริง และมีระบบ LocalStorage Fallback
- **วันที่และเวลาเป็น วัน เดือน ปี พ.ศ.**: ใช้วิธีแสดงผลปฏิทินไทยมาตรฐาน
- **หน่วยเงินสตางค์**: จัดเก็บในฐานข้อมูลเป็นจำนวนเต็ม (INTEGER) สตางค์เสมอ

