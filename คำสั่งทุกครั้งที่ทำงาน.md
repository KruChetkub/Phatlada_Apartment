เมื่อเปิด เครื่องที่ 2 (ก่อนเริ่มเขียนโค้ดต่อ):
ดึงโค้ดล่าสุดที่เพิ่งทำค้างไว้ลงมา:

bash

git pull


git add .
git commit -m "อัปเดตงานล่าสุด"
git push

git commit -m "เปลี่ยนชื่อเป็น Phatlada และปรับปรุงการแสดงผลมือถือ"
git push