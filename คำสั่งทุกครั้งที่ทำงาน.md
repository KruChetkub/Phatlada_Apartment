# คำสั่งและข้อกำหนดของโปรเจกต์ ภัทร์ลดา อพาร์ทเมนท์ (Phatlada Apartment)

## 1. กฎเหล็กเรื่อง วันที่และเวลา (Thai Buddhist Era Rule)
- **วันที่ทุกจุดในระบบต้องเป็น พ.ศ. (พุทธศักราช) และภาษาไทยเสมอ**:
  - การแสดงผลวันที่: ใช้ `formatThaiDateLong()` หรือ `formatThaiDateShort()` เช่น "5 ตุลาคม 2569" หรือ "5 ต.ค. 69"
  - การเลือกวันที่ (Date Picker): **ห้ามใช้ `<input type="date">` แบบภาษาอังกฤษเด็ดขาด** ให้ใช้คอมโพเนนต์ `<ThaiDatePicker />` ที่มีตัวเลือก วัน / เดือน (ภาษาไทย) / ปี (พ.ศ.) เท่านั้น

---

## 2. กฎเรื่องหน่วยเงิน (Monetary Units)
- ฐานข้อมูล (DB) จัดเก็บเป็นหน่วย **สตางค์ (Satang)**
- หน้าจอ (UI) ต้องแสดงผลเป็นหน่วย **บาท (฿)** โดยแปลงผ่านฟังก์ชัน `formatSatang()` หรือ `formatBaht(satangToBaht())` เสมอ

---

## 3. ขั้นตอนการทำงาน Git ในแต่ละเครื่อง

### เมื่อเปิดเครื่อง (ก่อนเริ่มเขียนโค้ดต่อ):
ดึงโค้ดล่าสุดที่เพิ่งทำค้างไว้ลงมา:
```bash
git pull
```

### เมื่อแก้ไขงานเสร็จ (ต้องการบันทึกและส่งขึ้น Vercel):
```bash
git add .
git commit -m "อัปเดตงานล่าสุด"
git push
```