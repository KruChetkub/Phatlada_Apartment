# RULES.md — กฎของโปรเจกต์ Phatlada

> กฎเหล่านี้ใช้กับทั้งคนและ AI agent ที่เขียนโค้ดในโปรเจกต์นี้
> **MUST** = ต้องทำ · **MUST NOT** = ห้ามทำ · **SHOULD** = ควรทำ ยกเว้นมีเหตุผล

---

## 1. ภาษา

- **MUST** ข้อความที่ผู้ใช้เห็นเป็นภาษาไทยทั้งหมด (ยกเว้นชื่อแบรนด์ Phatlada และ Premium)
- **MUST** ชื่อไฟล์ ตัวแปร ฟังก์ชัน คอมเมนต์ในโค้ด commit message เป็นภาษาอังกฤษ
- **MUST** เก็บข้อความ UI ไว้ในที่เดียว (`src/lib/i18n/th.ts` หรือไฟล์ข้อความของคอมโพเนนต์) ห้ามกระจายสตริงเดียวกันหลายที่
- **SHOULD** ใช้คำให้ตรงกันทั้งระบบ: ปุ่มที่เขียนว่า "บันทึกค่าเช่า" → toast ต้องเขียนว่า "บันทึกค่าเช่าแล้ว"

คำศัพท์มาตรฐาน:

| ใช้ | ห้ามใช้ |
|---|---|
| ผู้เช่า | ลูกค้า, ลูกหอ |
| ห้องพัก | ยูนิต |
| สัญญาเช่า | คอนแทรค |
| แจ้งซ่อม | แจ้งปัญหา, ticket |
| ชำระแล้ว / รอชำระ / เกินกำหนด | จ่ายแล้ว / ค้างจ่าย |
| มีผู้เช่า / ว่าง | ไม่ว่าง / available |

## 2. วันที่ เวลา และเงิน

- **MUST** เก็บวันเวลาใน DB เป็น UTC และแสดงผลด้วย timezone `Asia/Bangkok`
- **MUST** แสดงปีเป็น พ.ศ. ผ่าน `Intl.DateTimeFormat("th-TH")` เท่านั้น
- **MUST NOT** ใช้ `<input type="date">` แบบ native ของเบราว์เซอร์เด็ดขาด เพราะจะแสดงผลเป็น ค.ศ. และภาษาอังกฤษ ต้องใช้คอมโพเนนต์ `<ThaiDatePicker />` ที่เป็น วัน/เดือนไทย/ปี พ.ศ. เสมอ
- **MUST NOT** บวก/ลบ 543 ด้วยมือ และห้ามเก็บปี พ.ศ. ลง DB
- **MUST** เก็บเงินเป็นจำนวนเต็มหน่วย **สตางค์** (`Int`) ห้ามใช้ `float`
- **MUST** แปลงเงินผ่าน `lib/format.ts` เท่านั้น ห้ามต่อสตริง `"฿" + value` ในคอมโพเนนต์
- **MUST** ใช้ตัวเลขอารบิก (0–9) ไม่ใช้เลขไทย
- **MUST** เวลาแสดงแบบ 24 ชั่วโมง ต่อท้ายด้วย " น."

## 3. ดีไซน์และ UI

- **MUST** ยึด `DESIGN.md` และ `assets/reference-dashboard.png` เป็นแหล่งอ้างอิง
- **MUST** ใช้ design token (Tailwind theme / CSS variables) ห้ามใส่ hex ตรง ๆ ในคอมโพเนนต์
- **MUST** ใช้ radius, spacing ตามสเกลที่กำหนด ห้ามใช้ค่าตามใจ (`rounded-[13px]`, `p-[17px]`)
- **MUST NOT** เพิ่มสี ฟอนต์ เงา หรือแอนิเมชันที่ไม่มีใน DESIGN.md โดยไม่อัปเดต DESIGN.md ด้วย
- **MUST NOT** ใช้ตัวพิมพ์ใหญ่ทั้งหมด (ALL CAPS) กับ label
- **MUST** ไม่ใช้สีเป็นสิ่งเดียวที่สื่อความหมาย — ตัวบ่งชี้เปลี่ยนแปลงต้องมีลูกศร ↑/↓ และ badge ต้องมีข้อความ
- **SHOULD** ใช้คอมโพเนนต์จาก shadcn/ui ก่อนสร้างเอง

## 4. Accessibility

- **MUST** ปุ่มที่มีแต่ไอคอนต้องมี `aria-label` ภาษาไทย (เช่น "การแจ้งเตือน 3 รายการ")
- **MUST** ทุกจุดที่คลิกได้ใช้คีย์บอร์ดได้และมี focus ring มองเห็นชัด
- **MUST** ใช้ `<a>`/`<Link>` สำหรับการเปลี่ยนหน้า และ `<button>` สำหรับการกระทำ ห้ามใช้ `<div onClick>`
- **MUST** กราฟต้องมีข้อความสรุปสำหรับ screen reader (เช่น `aria-label` หรือตารางซ่อน)
- **MUST** contrast ข้อความอย่างน้อย 4.5:1 (ข้อความ muted บนพื้น soft ต้องตรวจ)
- **MUST** เคารพ `prefers-reduced-motion`
- **SHOULD** รูปภาพเนื้อหามี `alt` ภาษาไทย, รูปตกแต่งใช้ `alt=""`

## 5. โค้ด

- **MUST** TypeScript `strict: true` ห้าม `any` (ใช้ `unknown` แล้ว narrow)
- **MUST** ตรวจ input ทุกจุดที่มาจากภายนอก (form, query, body) ด้วย Zod
- **MUST** ฟังก์ชันคำนวณ KPI เป็น pure function ใน `lib/kpi.ts` และมี unit test
- **MUST** หน้า Dashboard และ API route ใช้ฟังก์ชันดึงข้อมูลชุดเดียวกัน
- **MUST NOT** hard-code ตัวเลขจากภาพดีไซน์ในคอมโพเนนต์ — ตัวเลขตัวอย่างอยู่ใน `prisma/seed.ts` เท่านั้น
- **MUST NOT** query DB ใน loop (N+1) — ใช้ `include` / `groupBy` / aggregate
- **SHOULD** ใช้ Server Components เป็นค่าเริ่มต้น ใช้ `"use client"` เฉพาะส่วนที่ต้อง interactive (กราฟ, dropdown)
- **SHOULD** คอมโพเนนต์ละไฟล์ ไม่เกินประมาณ 200 บรรทัด
- **SHOULD** ตั้งชื่อ: คอมโพเนนต์ `PascalCase`, ฟังก์ชัน/ตัวแปร `camelCase`, ค่าคงที่ `SCREAMING_SNAKE_CASE`, route `kebab-case`

## 6. ความปลอดภัยและข้อมูลส่วนบุคคล (PDPA)

- **MUST** ทุก query กรองด้วย `dormitoryId` ที่ผู้ใช้เป็นสมาชิก — ห้ามเชื่อ `dormitoryId` จาก client โดยไม่ตรวจสิทธิ์
- **MUST** ข้อมูลผู้เช่า (เบอร์โทร, เลขบัตรประชาชน, ที่อยู่) ไม่ปรากฏใน log
- **MUST** เลขบัตรประชาชนแสดงแบบปิดบัง `x-xxxx-xxxxx-12-3` ยกเว้นหน้ารายละเอียดที่มีสิทธิ์
- **MUST NOT** commit secret, `.env`, หรือข้อมูลผู้เช่าจริง
- **MUST NOT** ใช้ข้อมูลบุคคลจริงใน seed — ชื่อใน seed เป็นชื่อสมมติ
- **SHOULD** ลบรูป/ไฟล์ของผู้เช่าเมื่อสัญญาสิ้นสุดตามนโยบายการเก็บข้อมูลของหอ

## 7. Git และการทำงาน

- **MUST** commit แบบ Conventional Commits: `feat:`, `fix:`, `refactor:`, `test:`, `docs:`, `chore:`
- **MUST** ก่อน commit ต้องผ่าน `pnpm lint && pnpm typecheck && pnpm test`
- **MUST** เปลี่ยน schema ต้องมี migration และอัปเดต `SPEC.md` §4
- **MUST** เปลี่ยนดีไซน์ต้องอัปเดต `DESIGN.md`
- **SHOULD** 1 PR = 1 เรื่อง, มีภาพหน้าจอก่อน/หลังเมื่อแก้ UI

## 8. ห้ามทำโดยไม่ถามก่อน

- เพิ่ม dependency ใหม่
- เปลี่ยน tech stack หรือโครงสร้างโฟลเดอร์หลัก
- แก้ schema ที่ลบคอลัมน์หรือเปลี่ยนชนิดข้อมูล
- ลบไฟล์ใน `assets/` หรือไฟล์ `.md` ชุดนี้
- รันคำสั่งที่ลบข้อมูลใน DB ที่ไม่ใช่ local (`prisma migrate reset` บน staging/production)
